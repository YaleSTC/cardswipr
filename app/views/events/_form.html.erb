<%= simple_form_for(@event) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :title %>
    <%= f.input :description %>
    <%= f.association :event_attendance_entries %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
