<h1>Distribute USB Keys</h1>

<%= form_tag(distribution_lookup_path, remote: true) do %>
  Swipe ID Card or enter NetID: <%= text_field_tag(:query, nil, options = {:size => 12, :maxlength => 12}) %>
  <p>
  <%= @count%> USB keys have been handed out.
  </p>
<% end %>