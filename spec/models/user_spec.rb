require 'spec_helper'

RSpec.describe User, type: :model do
  xit '#get_ldap_attributes' do
    #call this on a random netid (csw3?)
    #does it return a hash with any attributes (name?)
  end

  xit '#full_name' do
    #create user
    #save user
    #does this return the expected value?
  end

  xit '#full_name_with_netid' do
    #create user
    #save user
    #does this return the expected value?
  end
end
